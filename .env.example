# PostgreSQL connection string (see docker-compose.yml for local credentials)
DATABASE_URL="postgresql://admin:password@localhost:5432/taskmanager"

# NextAuth (Auth.js v5) — generate with: openssl rand -base64 32
AUTH_SECRET=""
